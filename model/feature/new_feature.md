我现在的情况是这样的，我现在 @fcg_net.py 这个模块中，由于 FCG 的三级节点过多，且由于使用了 GNN 的信息传递功能，到反向传播时，传播链路特别复杂，速度太慢。现在我的想法是，干脆不使用信息传递功能，不改变 FCG 的节点特征，而将他作为一个静态的图去使用。取而代之的是，我要改变 VR 的特征，将 VR 的特征在 FCG 节点所处的语义空间进行对齐，然后在进行层级分类。
现在我比较纠结的几个点是：

1. 使用什么样的特征变换去调整 VR 的特征呢？
2. 是否要在 @fcg_builder.py 中保留特征维度为 900 维，因为每个 900 维的特征是来自 3 个词向量的拼接，保持 900 维就可以保持 FCG 节点特征在语义空间中的区分度。
3. 完全静态的 FCG 也不太好，是否要给三级节点加个统一的特征变换层，比如 Transformer 来对三级节点特征进行微调，进而将这种调整，延伸到 1、2 级节点？

关于问题的解决方案：
1. 使用交叉注意力
2. 保留更好
3. 使用自注意力对 FCG 三级节点特征微调